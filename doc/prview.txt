*prview.txt*                      Browse GitLab merge requests from Neovim

==============================================================================
CONTENTS                                                        *prview-contents*

1. Introduction                                              |prview-introduction|
2. Setup                                                            |prview-setup|
3. Command                                                        |:PRView|

==============================================================================
INTRODUCTION                                              *prview-introduction*

PRView browses open merge requests for the current repository's GitLab project.
The fzf-lua picker shows the complete GitLab-provided diff for the focused
merge request. PRView does not change local repository or GitLab state.

Required: glab and fzf-lua. Optional: delta for colored diff previews.
Authenticate GitLab requests with GITLAB_TOKEN or `glab auth login`. PRView
does not handle tokens itself.

==============================================================================
SETUP                                                            *prview-setup*

>lua
    require('prview').setup()
<

There are no feature-specific configuration options. Dependency checks and
external processes are deferred until the command is invoked.

==============================================================================
:PRView                                                          *:PRView*

Opens the open merge request picker. Highlight a merge request to preview its
complete, scrollable GitLab diff. Delta colors the diff when available; raw diff
syntax is the fallback. Press CTRL-O to open the highlighted merge request in
GitLab without closing the picker. ENTER does not close or replace the picker.

vim:tw=78:ts=8:noet:ft=help:norl:
